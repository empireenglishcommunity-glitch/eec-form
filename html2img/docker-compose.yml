version: '3.8'
services:
  html2img:
    build: .
    container_name: empire-html2img
    restart: unless-stopped
    ports:
      - "3200:3200"
    environment:
      - PORT=3200
    deploy:
      resources:
        limits:
          memory: 512M
